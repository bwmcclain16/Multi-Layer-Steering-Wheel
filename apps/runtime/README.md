# Runtime/HUB Note

The old separate runtime and hub concepts are being consolidated behind the two user-facing applications:
- `mlsw-dash` for the runtime/dashboard surface
- `mlsw-editor` for authoring and validation
